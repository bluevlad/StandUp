name: Deploy StandUp

on:
  push:
    branches:
      - prod

jobs:
  deploy:
    name: Deploy to Production
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          ref: prod

      - name: Create .env.production from secrets and variables
        env:
          # Secrets — 민감정보 (repo secret 유지)
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          GMAIL_ADDRESS: ${{ secrets.GMAIL_ADDRESS }}
          GMAIL_APP_PASSWORD: ${{ secrets.GMAIL_APP_PASSWORD }}
          REPORT_RECIPIENTS: ${{ secrets.REPORT_RECIPIENTS }}
          GH_TOKEN_VAL: ${{ secrets.GH_TOKEN }}
          # Variables — 비민감 설정값 (repo variables, ENVIRONMENT_STANDARD.md §8.4)
          GH_ORG_VAL: ${{ vars.GH_ORG }}
          LOG_LEVEL: ${{ vars.LOG_LEVEL }}
          # PR7 — tech_trend / HopenVision (vars 로 운영자 토글 가능). 기본은 안전한 off.
          STANDUP_MODE: ${{ vars.STANDUP_MODE }}
          TECH_TREND_ENABLED: ${{ vars.TECH_TREND_ENABLED }}
          TECH_TREND_KEYWORDS: ${{ vars.TECH_TREND_KEYWORDS }}
          HOPEN_BRIEF_DAILY_ENABLED: ${{ vars.HOPEN_BRIEF_DAILY_ENABLED }}
          HOPEN_BRIEF_FITNESS_THRESHOLD: ${{ vars.HOPEN_BRIEF_FITNESS_THRESHOLD }}
          # Phase 1 — TechBriefing 흡수 전환 (vars 로 운영자 토글, 기본 병행 발송 유지)
          INSIGHT_SEND_ENABLED: ${{ vars.INSIGHT_SEND_ENABLED }}
          INSIGHT_WEEKLY_TECH_SECTION_ENABLED: ${{ vars.INSIGHT_WEEKLY_TECH_SECTION_ENABLED }}
          # Phase 2b — LLMOps 보고 (standup-weekly-newsletter consumer)
          LLMOPS_URL: ${{ vars.LLMOPS_URL }}
          LLMOPS_API_KEY: ${{ secrets.LLMOPS_API_KEY }}
        run: |
          cat > .env.production << EOF
          DATABASE_URL=${DATABASE_URL}
          GMAIL_ADDRESS=${GMAIL_ADDRESS}
          GMAIL_APP_PASSWORD=${GMAIL_APP_PASSWORD}
          REPORT_RECIPIENTS=${REPORT_RECIPIENTS}
          GITHUB_TOKEN=${GH_TOKEN_VAL}
          GITHUB_ORG=${GH_ORG_VAL}
          LOG_LEVEL=${LOG_LEVEL:-INFO}
          API_PORT=9065

          # ── PR7 — tech_trend / HopenVision (모드·게이트 토글) ───────────
          STANDUP_MODE=${STANDUP_MODE:-both}
          TECH_TREND_ENABLED=${TECH_TREND_ENABLED:-true}
          TECH_TREND_KEYWORDS=${TECH_TREND_KEYWORDS:-java,spring,react,postgresql,typescript}
          # 컨테이너 안에서 마운트 결과 경로 (docker-compose.production.yml volumes 와 매칭).
          MEDIUM_DIGEST_REPORTS_DIR=/data/medium-digest
          HOPENVISION_REPO_PATH=/data/hopenvision
          # 일일 [HopenTechBrief] cron — vars 로 즉시 ON/OFF (PR8 에서 true 로 전환 예정).
          HOPEN_BRIEF_DAILY_ENABLED=${HOPEN_BRIEF_DAILY_ENABLED:-false}
          HOPEN_BRIEF_FITNESS_THRESHOLD=${HOPEN_BRIEF_FITNESS_THRESHOLD:-60}

          # ── TechBriefing 흡수 전환 (Phase 1) ────────────────────────────
          # false 로 내리면 주간 합성·저장·색인 유지, 단독 메일 발송만 생략
          INSIGHT_SEND_ENABLED=${INSIGHT_SEND_ENABLED:-true}
          # 주간 "이번 주 기술 토픽" 섹션 — TechBriefing 이관, 기본 비활성
          INSIGHT_WEEKLY_TECH_SECTION_ENABLED=${INSIGHT_WEEKLY_TECH_SECTION_ENABLED:-false}

          # ── Phase 2b — LLMOps 보고 ───────────────────────────────────────
          LLMOPS_URL=${LLMOPS_URL}
          LLMOPS_API_KEY=${LLMOPS_API_KEY}
          EOF

      - name: Run deploy script
        run: |
          chmod +x scripts/deploy.sh
          ./scripts/deploy.sh

      - name: Verify deployment
        run: |
          echo "Waiting for container to start..."
          sleep 15
          for i in 1 2 3 4 5; do
            if curl -sf http://localhost:9065/api/v1/health > /dev/null 2>&1; then
              echo "Health check passed!"
              exit 0
            fi
            echo "Attempt $i failed, retrying in 5s..."
            sleep 5
          done
          echo "Health check failed after 5 attempts"
          docker logs standup-app --tail 50
          exit 1

      - name: Cleanup .env.production
        if: always()
        run: rm -f .env.production
