version: "3.8"

services:
  standup:
    build: .
    container_name: standup-app
    ports:
      - "9065:9065"
    env_file:
      - .env.production
    environment:
      - TZ=Asia/Seoul
      - ROOT_PATH=/standup
    volumes:
      - ./logs:/app/logs
      # PR7 — tech_trend + HopenVision 입력 마운트 (운영 MacBook 절대 경로).
      # 컨테이너 안에서는 /data/* 로 일관 참조 — .env 의 MEDIUM_DIGEST_REPORTS_DIR
      # / HOPENVISION_REPO_PATH 와 1:1 매칭.
      - /Users/rainend/GIT/medium-digest-agent/reports:/data/medium-digest:ro
      - /Users/rainend/GIT/hopenvision:/data/hopenvision:ro
      # Claude 세션 transcript + 커밋 매핑 (Phase 2~3 claude-sessions ingest)
      # transcript 의 cwd 가 /Users/rainend/GIT/... 절대경로이므로
      # 컨테이너 안에서도 동일 경로로 보이도록 매핑한다.
      - /Users/rainend/.claude/projects:/root/.claude/projects:ro
      - /Users/rainend/GIT:/Users/rainend/GIT:ro
    restart: always
    networks:
      - app-network
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9065/api/v1/health')"]
      interval: 60s
      timeout: 10s
      start_period: 30s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "5"

networks:
  app-network:
    external: true
    name: unmong-network
